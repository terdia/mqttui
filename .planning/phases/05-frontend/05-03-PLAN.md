---
phase: 05-frontend
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - templates/partials/alerts_list.html
  - templates/partials/alert_row.html
  - templates/index.html
  - mqttui/routes/main.py
  - static/script.js
autonomous: true
requirements: [UI-04, UI-05]
must_haves:
  truths:
    - "User can see alert history in a dedicated Alerts tab"
    - "Alert history is paginated and loads more via htmx"
    - "Alerts can be filtered by rule and severity"
    - "Each alert shows timestamp, rule name, action type, status, and details"
  artifacts:
    - path: "templates/partials/alerts_list.html"
      provides: "htmx partial for alerts list with pagination"
      contains: "hx-"
    - path: "templates/partials/alert_row.html"
      provides: "Single alert row partial"
    - path: "mqttui/routes/main.py"
      provides: "Alerts partial route"
      contains: "alerts_list_partial"
  key_links:
    - from: "templates/partials/alerts_list.html"
      to: "mqttui/routes/main.py"
      via: "hx-get to fetch alerts partial with filters"
      pattern: "hx-get.*partials/alerts"
    - from: "mqttui/routes/main.py"
      to: "mqttui/rules/models.py"
      via: "AlertHistory.query for data"
      pattern: "AlertHistory.query"
    - from: "templates/index.html"
      to: "templates/partials/alerts_list.html"
      via: "Alerts tab loads partial via htmx"
      pattern: "hx-get.*partials/alerts"
---

<objective>
Build the Alert History UI panel as an htmx-powered tab with pagination, filtering by rule and severity, and status indicators for webhook delivery outcomes.

Purpose: Makes alert history visible to users without needing API tools. Completes the alerting loop from Phase 4 by giving users visibility into what alerts fired, whether webhooks succeeded, and what was suppressed.

Output: Alerts tab panel with paginated list, severity badges, filter controls, and htmx pagination.
</objective>

<execution_context>
@/Users/terryosayawe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/terryosayawe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend/05-CONTEXT.md
@.planning/phases/05-frontend/05-01-SUMMARY.md
@mqttui/routes/alerts.py
@mqttui/rules/models.py

<interfaces>
<!-- Alerts API (from mqttui/routes/alerts.py) -->
```
GET /api/v1/alerts/?page=1&per_page=20&rule_id=X&severity=Y
-> { status: "success", data: { alerts: [...], total, page, per_page } }
```

AlertHistory model fields (from mqttui/rules/models.py):
```python
id, rule_id, rule_name, topic, severity, message, fired_at,
webhook_url, http_status, retry_count, error_detail,
suppressed_count, cooldown_until
```

AlertHistory.to_dict() returns:
```python
{
    'id': int, 'rule_id': int, 'rule_name': str,
    'topic': str, 'severity': str, 'message': str,
    'fired_at': str (ISO), 'webhook_url': str|None,
    'http_status': int|None, 'retry_count': int,
    'error_detail': str|None, 'suppressed_count': int,
    'cooldown_until': str|None
}
```

<!-- Tab system from 05-01: activeTab in mqttuiApp() x-data, Alerts tab already has placeholder -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alert partial routes and alert list/row templates</name>
  <files>mqttui/routes/main.py, templates/partials/alerts_list.html, templates/partials/alert_row.html</files>
  <read_first>
    - mqttui/routes/main.py (existing routes including partials from 05-02)
    - mqttui/routes/alerts.py (API endpoint for reference)
    - mqttui/rules/models.py (AlertHistory model)
  </read_first>
  <action>
1. Add alert partial routes to `mqttui/routes/main.py`:
   ```python
   @bp.route('/partials/alerts')
   @login_required
   def alerts_list_partial():
       """Return HTML partial of alert history with pagination and filters."""
       from mqttui.rules.models import AlertHistory, Rule
       from mqttui.extensions import sa

       page = request.args.get('page', 1, type=int)
       per_page = min(request.args.get('per_page', 20, type=int), 100)
       rule_id = request.args.get('rule_id', type=int)
       severity = request.args.get('severity', type=str)

       query = AlertHistory.query.order_by(AlertHistory.fired_at.desc())
       if rule_id:
           query = query.filter(AlertHistory.rule_id == rule_id)
       if severity:
           query = query.filter(AlertHistory.severity == severity)

       total = query.count()
       alerts = query.offset((page - 1) * per_page).limit(per_page).all()
       has_next = (page * per_page) < total

       # Get all rules for filter dropdown
       rules = Rule.query.order_by(Rule.name).all()

       return render_template('partials/alerts_list.html',
                              alerts=alerts, rules=rules,
                              page=page, per_page=per_page,
                              total=total, has_next=has_next,
                              current_rule_id=rule_id,
                              current_severity=severity)
   ```
   Make sure `from flask import ... request` is imported (it already is from the existing code).

2. Create `templates/partials/alerts_list.html`:
   ```html
   <div id="alerts-list">
     <!-- Filters -->
     <div class="flex flex-wrap gap-3 mb-4 items-end">
       <div>
         <label class="block text-xs text-gray-400 mb-1">Rule</label>
         <select name="rule_id" id="alert-filter-rule"
                 hx-get="/partials/alerts"
                 hx-target="#alerts-list"
                 hx-swap="outerHTML"
                 hx-include="#alert-filter-severity"
                 class="rounded bg-gray-700 border-gray-600 text-white text-sm p-1.5">
           <option value="">All Rules</option>
           {% for rule in rules %}
           <option value="{{ rule.id }}" {{ 'selected' if current_rule_id == rule.id else '' }}>{{ rule.name }}</option>
           {% endfor %}
         </select>
       </div>
       <div>
         <label class="block text-xs text-gray-400 mb-1">Severity</label>
         <select name="severity" id="alert-filter-severity"
                 hx-get="/partials/alerts"
                 hx-target="#alerts-list"
                 hx-swap="outerHTML"
                 hx-include="#alert-filter-rule"
                 class="rounded bg-gray-700 border-gray-600 text-white text-sm p-1.5">
           <option value="">All</option>
           <option value="info" {{ 'selected' if current_severity == 'info' else '' }}>Info</option>
           <option value="warning" {{ 'selected' if current_severity == 'warning' else '' }}>Warning</option>
           <option value="critical" {{ 'selected' if current_severity == 'critical' else '' }}>Critical</option>
         </select>
       </div>
       <div class="text-sm text-gray-400">
         Showing {{ alerts|length }} of {{ total }} alerts
       </div>
     </div>

     <!-- Alert rows -->
     {% if alerts %}
     <div class="space-y-2">
       {% for alert in alerts %}
         {% include 'partials/alert_row.html' %}
       {% endfor %}
     </div>
     {% else %}
     <p class="text-gray-400 text-center py-8">No alerts recorded yet.</p>
     {% endif %}

     <!-- Pagination -->
     {% if has_next %}
     <div class="text-center mt-4">
       <button hx-get="/partials/alerts?page={{ page + 1 }}{% if current_rule_id %}&rule_id={{ current_rule_id }}{% endif %}{% if current_severity %}&severity={{ current_severity }}{% endif %}"
               hx-target="#alerts-list"
               hx-swap="outerHTML"
               class="px-4 py-2 bg-gray-700 hover:bg-gray-600 rounded text-sm">
         Load More (page {{ page + 1 }})
       </button>
     </div>
     {% endif %}
   </div>
   ```

3. Create `templates/partials/alert_row.html`:
   ```html
   <div class="bg-gray-700 rounded p-3 border-l-4
     {% if alert.severity == 'critical' %}border-red-500
     {% elif alert.severity == 'warning' %}border-yellow-500
     {% else %}border-blue-500{% endif %}">
     <div class="flex justify-between items-start">
       <div class="flex-1">
         <div class="flex items-center gap-2 mb-1">
           <span class="text-xs px-2 py-0.5 rounded
             {% if alert.severity == 'critical' %}bg-red-600
             {% elif alert.severity == 'warning' %}bg-yellow-600
             {% else %}bg-blue-600{% endif %}">
             {{ alert.severity }}
           </span>
           <span class="font-medium text-sm">{{ alert.rule_name or 'Unknown Rule' }}</span>
           {% if alert.webhook_url %}
           <span class="text-xs px-2 py-0.5 rounded
             {% if alert.http_status and alert.http_status < 400 %}bg-green-600
             {% elif alert.http_status %}bg-red-600
             {% else %}bg-gray-600{% endif %}">
             {% if alert.http_status %}HTTP {{ alert.http_status }}{% else %}Pending{% endif %}
           </span>
           {% endif %}
           {% if alert.suppressed_count > 0 %}
           <span class="text-xs text-gray-400">({{ alert.suppressed_count }} suppressed)</span>
           {% endif %}
         </div>
         <div class="text-sm text-gray-300">{{ alert.message or '' }}</div>
         <div class="text-xs text-gray-500 mt-1">
           Topic: <span class="text-blue-400">{{ alert.topic or 'N/A' }}</span>
           {% if alert.retry_count > 0 %}
           | Retries: {{ alert.retry_count }}
           {% endif %}
           {% if alert.error_detail %}
           | Error: <span class="text-red-400">{{ alert.error_detail[:100] }}</span>
           {% endif %}
         </div>
       </div>
       <div class="text-xs text-gray-400 whitespace-nowrap ml-2">
         {{ alert.fired_at.strftime('%Y-%m-%d %H:%M:%S') if alert.fired_at else '' }}
       </div>
     </div>
   </div>
   ```

4. Wire the Alerts tab in `templates/index.html`. The tab button was added in 05-02. Add the alerts panel:
   ```html
   <div x-show="activeTab === 'alerts'" class="flex-1 flex flex-col overflow-hidden">
     <div id="alerts-panel"
          hx-get="/partials/alerts"
          hx-trigger="load"
          hx-swap="innerHTML"
          class="flex-1 overflow-y-auto">
       <div class="text-center p-4 text-gray-400">Loading alerts...</div>
     </div>
   </div>
   ```

   Note: Use `hx-trigger="load"` so alerts load when the tab is first shown. If this causes an immediate load even when hidden, use `hx-trigger="intersect once"` instead, or use Alpine.js `x-init` with `htmx.ajax()`:
   ```html
   <div x-show="activeTab === 'alerts'" x-init="$watch('$store.mqtt', () => {})"
        x-effect="if (activeTab === 'alerts' && !alertsLoaded) { htmx.ajax('GET', '/partials/alerts', {target: '#alerts-panel', swap: 'innerHTML'}); alertsLoaded = true; }">
   ```
   Add `alertsLoaded: false` to mqttuiApp() x-data. This lazy-loads alerts only when the tab is first clicked.
  </action>
  <verify>
    <automated>cd /Users/terryosayawe/Code/mqttui && test -f templates/partials/alerts_list.html && test -f templates/partials/alert_row.html && grep -q "alerts_list_partial" mqttui/routes/main.py && grep -q "hx-get.*partials/alerts" templates/partials/alerts_list.html && grep -q "severity" templates/partials/alert_row.html && grep -q "alerts-panel" templates/index.html && echo "All checks pass"</automated>
  </verify>
  <acceptance_criteria>
    - test -f templates/partials/alerts_list.html
    - test -f templates/partials/alert_row.html
    - grep -q "alerts_list_partial" mqttui/routes/main.py
    - grep -q "AlertHistory" mqttui/routes/main.py
    - grep -q "hx-get.*partials/alerts" templates/partials/alerts_list.html
    - grep -q "severity" templates/partials/alert_row.html
    - grep -q "webhook_url" templates/partials/alert_row.html
    - grep -q "suppressed_count" templates/partials/alert_row.html
    - grep -q "alerts-panel" templates/index.html
    - grep -q "has_next" templates/partials/alerts_list.html
  </acceptance_criteria>
  <done>Alerts panel is a dedicated tab that loads alert history via htmx. Each alert shows severity badge, rule name, webhook status, suppressed count, error details, and timestamp. Filter dropdowns for rule and severity trigger htmx partial reload. Pagination loads more results via htmx.</done>
</task>

</tasks>

<verification>
- Alert partials exist and contain htmx attributes
- Flask partial route queries AlertHistory with pagination
- Filter selects use hx-get to reload the list
- Pagination button exists with hx-get for next page
- Alert row shows all key fields: severity, rule_name, http_status, suppressed_count
</verification>

<success_criteria>
1. Alerts tab exists in the tab bar alongside Messages and Rules
2. Clicking Alerts tab loads alert history via htmx (lazy-loaded)
3. Each alert row shows severity badge (color-coded), rule name, delivery status, timestamp
4. Filter by rule dropdown reloads the list
5. Filter by severity dropdown reloads the list
6. Pagination "Load More" button fetches the next page
7. Suppressed count and retry count are visible when non-zero
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend/05-03-SUMMARY.md`
</output>
